---
title: Configuration Reference
description: Complete guide to YAML configuration options for load testing
---

# Configuration Reference

Learn how to configure your load tests using YAML configuration files.

## Basic Configuration

All load tests are configured using YAML files in the `configs/` directory. Here's a complete example:

```yaml
# Target URL to test (any web application)
target: https://example.com/contact

# Number of virtual users (concurrent users)
vus: 50

# Test duration (supports: 30s, 1m, 5m, 1h, etc.)
duration: 30s

# Site description for AI analysis (optional)
description: "Example website for load testing"

# Tags for categorization and AI analysis (optional)
tags:
  - "example"
  - "contact-form"
  - "static-site"
```

## Configuration Options

### Required Fields

<Accordion>
  <AccordionItem title="target">
    **Type**: `string`  
    **Required**: Yes  
    **Description**: The URL to test (any web application)
    
    ```yaml
    target: https://example.com/contact
    target: https://api.example.com/v1/users
    target: https://wordpress.org/
    ```
  </AccordionItem>
  
  <AccordionItem title="vus">
    **Type**: `integer`  
    **Required**: Yes  
    **Description**: Number of virtual users (concurrent users)
    
    ```yaml
    vus: 10    # Light load
    vus: 50    # Medium load
    vus: 100   # Heavy load
    vus: 500   # Stress test
    ```
  </AccordionItem>
  
  <AccordionItem title="duration">
    **Type**: `string`  
    **Required**: Yes  
    **Description**: Test duration in k6 format
    
    ```yaml
    duration: 30s    # 30 seconds
    duration: 1m     # 1 minute
    duration: 5m     # 5 minutes
    duration: 1h     # 1 hour
    ```
  </AccordionItem>
</Accordion>

### Optional Fields

<Accordion>
  <AccordionItem title="description">
    **Type**: `string`  
    **Required**: No  
    **Description**: Site description for AI analysis context
    
    ```yaml
    description: "Frontend is built with Svelte and the Backend is built with Strapi. The frontend application gets its data from the strapi api. This app is an App Service that lives on azure."
    ```
  </AccordionItem>
  
  <AccordionItem title="tags">
    **Type**: `array`  
    **Required**: No  
    **Description**: Technology tags for AI analysis
    
    ```yaml
    tags:
      - "svelte"
      - "strapi"
      - "azure"
      - "app-service"
      - "headless-cms"
      - "javascript"
      - "api-driven"
    ```
  </AccordionItem>
</Accordion>

## Example Configurations

### Basic Website Test

```yaml
# configs/basic-test.yaml
target: https://example.com/contact
vus: 25
duration: 2m
description: "Basic website contact form testing"
tags:
  - "static-site"
  - "contact-form"
  - "html"
```

### WordPress Site

```yaml
# configs/wordpress-test.yaml
target: https://wordpress.org/
vus: 25
duration: 2m
description: "Official WordPress.org website - a popular content management system built with PHP"
tags:
  - "wordpress"
  - "php"
  - "cms"
  - "content-management"
  - "open-source"
```

### API Endpoint

```yaml
# configs/api-test.yaml
target: https://jsonplaceholder.typicode.com/posts
vus: 100
duration: 1m
description: "JSONPlaceholder API - a free fake API for testing and prototyping REST endpoints"
tags:
  - "api"
  - "rest"
  - "json"
  - "testing"
  - "prototype"
  - "fake-api"
```

### Modern Web Application

```yaml
# configs/pop-website-test.yaml
target: https://pop-website-2024-dev.azurewebsites.net
vus: 25
duration: 2m
description: "Frontend is built with Svelte and the Backend is built with Strapi. The frontend application gets its data from the strapi api. This app is an App Service that lives on azure."
tags:
  - "svelte"
  - "strapi"
  - "azure"
  - "app-service"
  - "headless-cms"
  - "javascript"
  - "api-driven"
```

## Load Testing Scenarios

### Light Load Testing

```yaml
vus: 10
duration: 1m
```

**Use Case**: Basic functionality testing, development environments

### Medium Load Testing

```yaml
vus: 25
duration: 2m
```

**Use Case**: Production testing, performance validation

### Heavy Load Testing

```yaml
vus: 100
duration: 5m
```

**Use Case**: Stress testing, capacity planning

### Stress Testing

```yaml
vus: 500
duration: 10m
```

**Use Case**: Breaking point testing, infrastructure validation

## Duration Formats

k6 supports various duration formats:

| Format | Example | Description |
|--------|---------|-------------|
| Seconds | `30s` | 30 seconds |
| Minutes | `2m` | 2 minutes |
| Hours | `1h` | 1 hour |
| Combined | `1h30m` | 1 hour 30 minutes |

## Best Practices

### 1. Start Small

```yaml
# Start with light load
vus: 10
duration: 30s
```

### 2. Gradual Increase

```yaml
# Test different load levels
vus: 10   # Light
vus: 25   # Medium  
vus: 50   # Heavy
vus: 100  # Stress
```

### 3. Realistic Duration

```yaml
# Short for development
duration: 30s

# Medium for testing
duration: 2m

# Long for production
duration: 5m
```

### 4. Descriptive Tags

```yaml
tags:
  - "react"           # Frontend framework
  - "nodejs"          # Backend technology
  - "aws"             # Infrastructure
  - "e-commerce"      # Application type
  - "api-driven"      # Architecture pattern
```

### 5. Clear Descriptions

```yaml
description: "E-commerce site built with React frontend and Node.js API backend, hosted on AWS with CDN and database optimization."
```

## Configuration Validation

The system validates your configuration:

- ✅ **Required fields** present
- ✅ **Data types** correct (string URL, integer VUs, string duration)
- ✅ **URL format** valid
- ✅ **Positive values** for VUs and duration
- ✅ **Optional fields** with defaults

## Running Tests

```bash
# Use default configuration
python run_test.py

# Use specific configuration
python run_test.py configs/my-app.yaml

# Use custom configuration
python run_test.py /path/to/custom-config.yaml
```

## Environment Variables

You can also use environment variables for dynamic configuration:

```bash
# Override configuration values
export TARGET_URL="https://my-app.com"
export VUS=50
export DURATION="5m"

# Run test
python run_test.py configs/template.yaml
```

## Advanced Configuration

### Custom k6 Options

Future versions will support additional k6 configuration options:

```yaml
# Coming soon
timeout: 10s
ramp_up: 10s
think_time: 1s
thresholds:
  http_req_duration: ["p(95)<2000"]
  http_req_failed: ["rate<0.1"]
```

### Multiple Scenarios

```yaml
# Coming soon
scenarios:
  - name: "Ramp up"
    vus: 0
    target: 50
    duration: 2m
  - name: "Sustained load"
    vus: 50
    duration: 5m
  - name: "Ramp down"
    vus: 50
    target: 0
    duration: 1m
```

---

<div className="cta">
  <h3>Ready to create your configuration?</h3>
  <p>Start with one of our examples and customize it for your application.</p>
  <a href="/examples" className="btn btn-primary">View Examples</a>
</div> 